/target
**/target
.env
*.swp
*.swo
*~
.DS_Store
.idea/
.vscode/
*.pdb
.cargo-cache/
spikes/*/Cargo.lock
# local EC2 / tooling scratch
/tmp/
*.tgz
# Hugging Face / llama.cpp model weights (BGE-M3 etc.)
/models/
*.gguf
*.bin
*.safetensors

# git worktrees (star model task checkouts)
/worktrees/

# local MCP client config (carries a cluster id)
.mcp.json

# Python bytecode from scripts/ (gen-fixtures.py, scripts/aws-infra/*.py)
__pycache__/
*.pyc
lambo.toml
scripts/recording/node_modules/
evidence/cloudops-video/
evidence/video-raw/

# Container build tree (scripts/aws-infra deploy pipeline)
target-docker/

# Agent worktrees and local agent config
.claude/

# Author's working documents (script drafts) and LibreOffice lock files
/lambo-script.odt
.~lock.*#

# Runtime pid file written by the local serve helper
serve.pid

# SQLite sidecars left behind by reading a committed evidence store
*.db-shm
*.db-wal
