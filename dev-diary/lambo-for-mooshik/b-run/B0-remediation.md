# B0 round-1 remediation

**Agent:** `b0-remediator`. Branch `b0-pg-extraction`. Tree left dirty, **not
committed**. Closes every finding in
`dev-diary/adversarial-review/adve-review-mooshik-B-B0-round1.md`. Does not
re-litigate B-postgres-store.md, does not split `init_schema` or
`connect_options`, does not start Postgres, does not run live `#[ignore]`d
Cockroach tests.

Authority: spec of record, then B-postgres-store.md, then source, then the
review file.

---

## Per-finding

### B0-R1-1 (P2): closed

Standing pin landed in `src/store/pg/cockroach.rs` `mod tests`, next to the
other SQL-shape tests:

* test: `b0_composed_sql_is_byte_identical_to_the_pre_carve_constants`
  (`src/store/pg/cockroach.rs:662`)
* `PRE_*` bodies generated by a parser over
  `git show 7937de7:src/store/cockroach.rs`, not transcribed by hand
* asserts all 10 `DialectSql` fields, `vector_cast == "::VECTOR"`, and
  `keyword_candidates_sql::<CockroachDialect>(n)` for n = 1..5 against the
  pre-carve builder copied from that commit
* CYCLE.md listed counts updated to **939 / 598 / 1007**, naming this test

The pin fails on a single whitespace change to any composed statement, and on
a change to `STRING_CAST`, `VECTOR_CAST`, or `DISTANCE_OP`. Mutation evidence
below. Reverted after each cycle.

### B0-R1-2 (P3): closed

1. `src/store/pg/cockroach.rs:114`: `[PgStore::init_schema]` →
   `[crate::store::GraphStore::init_schema]` (the method is on the trait).
2. `src/store/pg/mod.rs:940`: `[CockroachStore::normalize_tokens]` →
   `[PgStore::normalize_tokens]` (the method lives on `PgStore`).

`cargo doc --no-deps --document-private-items --features store-cockroach,fixtures`
added to CYCLE.md phase gates (`CYCLE.md:85`). Measured: **54** rustdoc
warnings, matching the `7937de7` baseline; **zero** naming `store/pg`,
`init_schema`, `normalize_tokens`, or `CockroachStore`. The two new warnings
are gone.

### B0-R1-3 (P3): closed (description only)

`init_schema` and `connect_options` were **not** split. `B0-implementation.md`
§3.4 (`:182`) and new §4.1 (`:255`) now say they are known over-merged debt
owed a split at B2/B3: executed DDL (`endpoint STRING`) and a Cockroach
session setting (`vector_search_beam_size`) that are not byte-identical for
Postgres. They are no longer a flat list of five error strings. B2 inherits
the debt.

### B0-R1-4 (P3): closed

B0-N2 row now names both sites (`B0-implementation.md:278`):
`PgStore::new` (`src/store/pg/mod.rs:1201`) **and** `connect_options`
(`src/store/pg/mod.rs:1234`). Same `"invalid Cockroach DSN: {e}"` string, two
paths.

### B0-R1-5 (P3): already-closed

Verified, nothing missing. CYCLE.md baseline table already has the fixtures
row: 1006 listed / 994 passed / 12 ignored, plus the note that `fixtures`
compiles `mod conformance` and `mod h2_cockroach_parity` (`CYCLE.md:44-56`).
The two listing files promised at `$SCRATCH/b-baseline/` exist
(`tests-cockroach.txt`, `tests-nodefault-cockroach.txt`). No third fixtures
listing was named; none invented. Recorded as **closed before this
remediation**.

### B0-R1-6 (P3): closed

`B0-implementation.md:105`: "52 of the 938 entries" → **26** of 938 (26 of
597, 36 of 1006). Conclusion unchanged: full-path listing diffs empty after
the one module-prefix rewrite.

---

## Mutations (R1-1)

Each cycle: mutate, run
`--lib store::pg::cockroach::tests::b0_composed_sql_is_byte_identical_to_the_pre_carve_constants --exact`,
revert. Constants restored to `STRING_CAST = "::STRING"`,
`DISTANCE_OP = "<->"`, `VECTOR_CAST = "::VECTOR"` afterwards.

| # | Mutation | Result | Panic |
| --- | --- | --- | --- |
| M5 | `STRING_CAST` `"::STRING"` → `"::TEXT"` | **RED** | `cockroach.rs:751` `vector_candidates`: left `id::TEXT` / `session_id::TEXT`, right `id::STRING` / `session_id::STRING` |
| M7 | `DISTANCE_OP` `"<->"` → `"<=>"` | **RED** | `cockroach.rs:751` `vector_candidates`: left `embedding <=> $1::VECTOR`, right `embedding <-> $1::VECTOR` |
| WS | `ORDER BY dist ASC` → `ORDER BY  dist ASC` in the `vector_candidates` template (`pg/mod.rs`) | **RED** | `cockroach.rs:751` `vector_candidates`: left `ORDER BY  dist ASC`, right `ORDER BY dist ASC` |

M5 and M7 are the two Part C survivors the deleted proof was the only pin
for. WS shows the pin is not a substring check: one extra space fails it.
`VECTOR_CAST` is covered by the same `vector_candidates` / `vector_cast`
asserts (`$1::VECTOR` and `assert_eq!(sql.vector_cast, "::VECTOR")`).

---

## Gates (Claimed vs Measured)

Re-run by this agent on the remediating tree. Doctest and integration-test
bins are included in `cargo test` totals, matching the reviewer's accounting.

| Gate | Claimed | Measured |
| --- | --- | --- |
| `cargo fmt --all -- --check` | pass | **pass** (rc 0) |
| `cargo clippy --all-targets -- -D warnings` | pass | **pass** (rc 0) |
| `cargo clippy --all-targets --features store-cockroach -- -D warnings` | pass | **pass** (rc 0) |
| `cargo test --features store-cockroach` | 939 listed, 935 passed / 4 ignored | **939 listed**, 935 passed / 0 failed / 4 ignored (rc 0) |
| `cargo test --no-default-features --features store-cockroach` | 598 listed | **598 listed**, 598 passed / 0 failed / 0 ignored (rc 0) |
| `cargo test --features store-cockroach,fixtures` | 1007 listed, 995 passed / 12 ignored | **1007 listed**, 995 passed / 0 failed / 12 ignored (rc 0) |
| H1 lock: `--lib h1_cross_store_parity` under `store-sqlite,store-cockroach,fixtures` | green; `git diff src/store/sqlite.rs` empty | `h1_sqlite_and_memory_oracle_agree_exactly` **passed**; sqlite.rs diff **empty** (0 bytes) |
| `cargo doc --no-deps --document-private-items --features store-cockroach,fixtures` | two new warnings gone | **54** warnings (baseline 54, post-B0 was 56); **none** naming `store/pg` / `init_schema` / `normalize_tokens` / `CockroachStore` (rc 0) |

Live `#[ignore]`d Cockroach tests: **not run**. Postgres container: **not
started**.

---

## Closures table

| ID | Grade | Status | Where |
| --- | --- | --- | --- |
| B0-R1-1 | P2 | **closed** | `src/store/pg/cockroach.rs:662`; CYCLE.md 939 / 598 / 1007 |
| B0-R1-2 | P3 | **closed** | `cockroach.rs:114`, `pg/mod.rs:940`, CYCLE.md phase-gate list |
| B0-R1-3 | P3 | **closed** | `B0-implementation.md` §3.4 / §4.1 (no code split) |
| B0-R1-4 | P3 | **closed** | `B0-implementation.md` B0-N2 row, both sites |
| B0-R1-5 | P3 | **already-closed** | `CYCLE.md:44-56`; listing files present at `$SCRATCH/b-baseline/` |
| B0-R1-6 | P3 | **closed** | `B0-implementation.md:105` |

B0 is not closed. The next step is round-2 review.
