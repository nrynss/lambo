# Adversarial Review — v0.2.0 release changes (Round 2)

- **Scope**: remediated worktree `/home/nryn/work/worktrees/remed-v020` (detached HEAD at `85b5c79`), 8 modified files + the round-1 review doc. Re-review of the FULL current diff against the 7 round-1 remediations and the honest flags; last gate before tagging v0.2.0.
- **Mode**: READ-ONLY on source (the round-1 doc and this file are the only writes). Targeted checks only; Main verifies the full suite.
- **Verdict**: **REQUEST_CHANGES** — 0× P1, 0× P2, 1× P3, 4× nits. Six of the seven remediations are verified genuine and complete with no regressions; the P3-4 sweep is **incomplete on the deployed docs site**: the page users actually see (`site/src/content/docs/installation.mdx`, built by docs.yml) still pins `LAMBO_VERSION=0.1.0` and still lists the unbuilt `macOS x86_64` platform — the exact "docs-site pin" round-1's P3-4 fix note named. Everything else is APPROVE-level; the fix is ~2 lines plus an optional URL repair.
- **Disposition**: apply v020-R2-P3-1 (re-copy/fix the site copy per `site/README.md`, fix the two broken URLs in `docs/reference/installation.mdx` in the same pass) and the four nits; then tag v0.2.0. No P1/P2, no regressions found anywhere.

## Method / evidence (all run in this worktree; cargo 1.97.1)

| Check | Result |
|---|---|
| `git diff HEAD` (full) | 8 files, +90/−22: Cargo.toml (+23 incl. include whitelist + default-features change), release.yml (+37 incl. notes render + publish-crate), ci.yml (+13 matrix), release-notes-template.md, README.md, installation.mdx, launch_exhibit_ec2.py, Cargo.lock (1 line) |
| `python3 yaml.safe_load` (ci.yml + release.yml) | Both parse; jobs `check/feature-matrix/cockroach-live` and `build/release/publish-crate`; `publish-crate` needs `release`, perms `{contents: read}` (PyYAML's `on:` bool quirk noted in round 1 is parser-only) |
| `cargo publish --dry-run --allow-dirty` | **Packaged 103 files, 3.0 MiB (768.5 KiB compressed); verify build compiled; upload aborted (dry run)**. Verify build proves the `include_str!` inputs (web/, migrations/, lambo.example.toml) are present |
| `cargo package --list --allow-dirty` | 103 files: whitelist (`src/**`, `web/**`, `migrations/**`, `fixtures/**`, `examples/**`, `lambo.example.toml`, `rust-toolchain.toml`, `README.md`, `LICENSE`, `NOTICE`) + 4 autos (Cargo.toml/.orig, Cargo.lock, `.cargo_vcs_info.json`) + **15 README paths** (12 distinct files: root README.md + 11 distinct nested; `dev-diary/evidence/*` are symlink dups of `evidence/*`). **Zero** tests/, dev-diary content, evidence content, scripts/.py, site/ content, .grok, .github, AGENTS.md, .env.example |
| Nested-README residual (honest flag a) | **Genuinely cargo-mandated, empirically proven**: scratch-crate tests in /tmp showed (A) no readme key + no root README → nested READMEs NOT packaged; (B) `readme` key set → nested READMEs auto-packaged; (C) `exclude = ["nested/README.md"]` does **not** suppress them; (D) no readme key but root README.md present (cargo auto-detects) → still auto-packaged. The trigger is the root README.md itself, which must ship (crates.io page). The only escapes are deleting the root README from the package or renaming every nested README repo-wide — neither is viable. **Non-suppressible: confirmed.** |
| Credential scan of `target/package/lambo-0.2.0/` | Clean: no AKIA/private keys/certs/ARNs/cockroachlabs host/DSN values. Hits are placeholders (`lambo.example.toml` `USER:PASSWORD@HOST` template), prose ("password reset" fixture content, RDS prose), logical names (`Subnet-Public-1a` = name, not `subnet-0…` ID). One `/home/nryn` path: `evidence/mcp-client-stdio/README.md:25` (also via the symlink dup). `dev-diary/adversarial-review/README.md:10` names cluster `nrynss` + commit `b45c102` in a scope line — public artifacts, no endpoint. |
| P3-1 (toolchain) | release.yml:245-250: `toolchain:` input dropped; comment explains rust-toolchain.toml (1.97.1) governs and that the `# 1.97.1` comment pins the action code revision. Matches ci.yml/build jobs. publish-crate still correct: needs `release`, `contents: read`, `CARGO_REGISTRY_TOKEN` env, no `--features`, tag-only trigger. |
| P3-2 (notes render) | Template uses `{{VERSION}}` (no 0.1.0); release.yml:210-218 renders via `sed "s/{{VERSION}}/$VERSION/g"` to `/tmp/notes-$VERSION.md`; `--notes-file /tmp/notes-$VERSION.md` (line 232). Rendered copy verified: **0 leftover placeholders, 0 `0.1.0`, 0 macos-x86_64**; asset table = linux-x86_64/linux-arm64/macos-arm64/windows-x86_64 all `0.2.0`; install URL `v0.2.0`. |
| P3-3 (fixtures matrix — highest-risk) | Correct and complete. Feature set per row: check `cargo test --all --features fixtures`; sqlite `cargo test --features store-sqlite,fixtures`; sqlite-minimal/minimal/cockroach `--no-default-features` unchanged; demo `cargo check --features demo` unchanged; cockroach-live `--features store-cockroach,store-memory,fixtures --lib -- --ignored` unchanged. Coverage map from the cfg inventory (24 files gate on `fixtures`): memory+fixtures tests (canon stage1-3, daemon gc/drift/conflict/hotlist/mod/score, graph, recall, store/memory, store/load, canon/eval `all(memory,fixtures)`) → check row; store/sqlite.rs (8 gates) → sqlite row; store/cockroach.rs conformance mod (`all(test, store-cockroach, fixtures)`, #[ignore]d live) → cockroach-live row; `tests/rebuild_session.rs` is `#![cfg(feature = "fixtures")]` file-gated → check + sqlite rows. **No fixtures-gated test lost from any row** vs the pre-change baseline (default included fixtures; the two edited rows now enable it explicitly with identical feature sets). |
| Regression runs | `cargo test --all --features fixtures` **PASS**; `cargo test --features store-sqlite,fixtures` **PASS**; `cargo test --all-features` **820 passed, 0 failed, 8 ignored** (the 8 are live-DSN cockroach, expected); `cargo check --features demo` **PASS**; `python3 -m py_compile launch_exhibit_ec2.py` OK. Proof the fixtures tests really execute: `cargo test --lib --features fixtures` = **746** passed vs `--lib` (no fixtures) = **691** — 55 fixtures-gated tests only run with the feature, and the check row enables it. |
| P3-4 (version sweep) | **PARTIAL — the only un-remediated round-1 item.** Fixed: `launch_exhibit_ec2.py:94` default → `0.2.0` ✓; `docs/reference/installation.mdx` version → `0.2.0` and macOS x86_64 row dropped ✓; `site/package.json` skip is reasonable (npm metadata; docs.yml deploys on main, not per tag) ✓. **Missed: `site/src/content/docs/installation.mdx`** — the file docs.yml actually builds (docs.yml: `working-directory: site`, `npm run build`, uploads `site/dist`). It still has `LAMBO_VERSION=0.1.0` (line 34) and still lists `macOS x86_64` (line 12). `site/README.md:11,57` documents the convention: site pages are **copied from `docs/reference/`** and must be re-copied when the reference changes — the re-copy was not done. Also missed: `scripts/aws-infra/README.md:164` documents the launch default as `0.1.0` (now wrong; **this file ships in the crates.io tarball**) and `launch_exhibit_ec2.py:38` docstring example `--lambo-version 0.1.0`. |
| Version consistency | Cargo.toml `0.2.0`, Cargo.lock lambo entry `0.2.0`, tag assertion dynamic (`v$VERSION` from Cargo.toml, release.yml:197-202). No stray 0.1.0 in src/, workflows, README, release-notes template, or installation.mdx (both copies' pins are the sweep items above). `scripts/install.sh` is version-agnostic (resolves latest or `$LAMBO_VERSION`). |
| Honest flag b (broken URL) | Confirmed pre-existing, and **lower impact than the brief assumed**: the broken URL is in `docs/reference/installation.mdx:17` (`github.com/nrynss/releaseslatest/…` — missing `/lambo/`) and a second one at line 32 (`raw.githubusercontent.com/nrynss/mainscripts/install.sh`), but that file is NOT deployed (docs.yml builds `site/` only; the deployed site copy has the correct URLs at both spots). Non-blocking; should be repaired when the site copy is refreshed (a blind re-copy per `site/README.md` would import the broken URLs). |
| Honest flag c (ci.yml path filter omits web/)** | Confirmed pre-existing: ci.yml has **zero** `web` references; the path-filter comment names "three non-Rust trees" but `web/` is a fourth real compile input (`src/cli/serve_web.rs:117-119` `include_str!`s `../../web/index.html`, `app.css`, `app.js` — all three exist and the packaged verify build compiled). A web-only edit therefore skips CI. Separate concern, non-blocking. |
| Round-1 nits | N1 resolved by P3-1 (comment now explains the action-code pin vs toolchain). N2 resolved by the whitelist (`rust-toolchain.toml` kept deliberately with a comment). N3/N4: no change needed, re-verified accurate. |

## Findings

### P1 — none

### P2 — none

### P3-1 — P3-4 sweep incomplete: deployed docs page still pins 0.1.0 and lists the unbuilt macOS x86_64; two more stale 0.1.0 in the aws-infra tree
- **file:line**: `site/src/content/docs/installation.mdx:34` (`LAMBO_VERSION=0.1.0 …`), `:12` (platform list still includes `macOS x86_64`); `scripts/aws-infra/README.md:164` ("default `0.1.0`"); `scripts/aws-infra/launch_exhibit_ec2.py:38` (usage docstring `--lambo-version 0.1.0`).
- **what**: Round-1's P3-4 fix note named the docs-site pin ("updated by docs.yml on main") as a sweep target. The remediation bumped the launch-script default and the `docs/reference/installation.mdx` copy, but the file docs.yml actually deploys — `site/src/content/docs/installation.mdx` — was not touched, violating the documented re-copy procedure (`site/README.md:11,57`). After v0.2.0 ships and the next main push redeploys the docs, the live install page (nrynss.github.io/lambo/installation/) will still instruct `LAMBO_VERSION=0.1.0` (installs the old binary) and advertise a `macOS x86_64` asset that v0.2.0 will not build. The two aws-infra leftovers are the same class of stale-version drift in the swept tree; the README one additionally ships inside the immutable crates.io tarball (verified in `cargo package --list`).
- **why P3, not nit**: it is an explicitly-named round-1 sweep item that the remediation claimed delivered but is not; it is user-facing on the deployed site; and the repo's own disposition required all P3s cleared before tagging.
- **fix** (small): in `site/src/content/docs/installation.mdx` set `LAMBO_VERSION=0.2.0` and drop `macOS x86_64` from the platform list (line 12) — per `site/README.md` this is the "re-copy" step; do **not** blind-copy from `docs/reference/installation.mdx`, because that file carries two pre-existing broken URLs (lines 17, 32) — repair those two URLs in the reference in the same pass, then re-copy. Also bump `scripts/aws-infra/README.md:164` and `launch_exhibit_ec2.py:38` to `0.2.0` (or generalize the examples).

### Nits
- **v020-R2-N1** — `scripts/install.sh:12` comment example still says `e.g. "0.1.0"` for `LAMBO_VERSION`. It is a generic "e.g." value, so it is harmless, but the same version sweep could modernize it to `0.2.0`.
- **v020-R2-N2** — count deltas vs the remediation brief (cosmetic, no judgment impact): the tarball ships **15 README paths = 12 distinct files** (root + 11 distinct nested; `dev-diary/evidence/*` are symlink dups of `evidence/*`), not "12 nested"; and exactly **one** distinct `/home/nryn` path ships (`evidence/mcp-client-stdio/README.md:25`), not two.
- **v020-R2-N3** — stale bytecode: `scripts/aws-infra/__pycache__/launch_exhibit_ec2.cpython-314.pyc` still embeds `0.1.0` (pre-fix compile). Untracked, not shipped, regenerated on next import — delete it in the same cleanup pass so a future `grep 0.1.0` of the tree is clean.
- **v020-R2-N4** — pre-existing out-of-scope items confirmed, none blocking: (a) the two broken URLs in `docs/reference/installation.mdx:17,32` (non-deployed copy; the live site copy has correct URLs); (b) ci.yml path filters omit `web/**`, so web-only changes skip CI (a fourth real compile-input tree; follow-up to the path-filter comment which names only three).

## Remediation-genuineness verdict (item by item)

1. **v020-R1-P2-1** — GENUINE. 103-file whitelist package, 3.0 MiB, verify build compiles, no creds/identifiers (scanned), `web/**` addition is a real catch (`serve_web.rs` embeds exactly those three files). Nested-README residual is genuinely cargo-mandated (proven, non-suppressible even via `exclude`; content is doc indexes + one `/home/nryn` prose path + a cluster-name mention — all acceptable).
2. **v020-R1-P3-1** — GENUINE. Input dropped, comment aligned, publish-crate job still correct.
3. **v020-R1-P3-2** — GENUINE. Placeholder + sed render + rendered notes verified clean (no 0.1.0, no macos-x86_64, no leftover placeholder).
4. **v020-R1-P3-3** — GENUINE AND COMPLETE. Matrix preserves every fixtures-gated test (55 lib tests + rebuild_session.rs + sqlite fixtures tests verified running and passing in the right rows; no-default rows and demo/cockroach-live unchanged); all-features 820/0/8.
5. **v020-R1-P3-4** — **INCOMPLETE** (v020-R2-P3-1 above).
6. **N1/N2** — resolved.

## Summary

This is a nearly-clean round: the P2 package-scoping fix is exactly right (103 files, ~3 MiB, no internal content, verify build green, nested-README residual proven non-suppressible and harmless), the CI matrix surgery preserves all fixtures coverage (746 vs 691 lib tests proves the gated tests run; all matrix rows that must pass do), the notes render correctly, toolchain pinning is honest, and there are no regressions across `--all --features fixtures`, the sqlite row, and `--all-features`. The one genuine miss is the P3-4 version sweep: the **deployed** docs page still pins 0.1.0 and lists the unbuilt macOS x86_64 row (plus two stale 0.1.0 in the aws-infra tree, one of which ships in the crates tarball) — the exact sweep round-1 named. Fix those ~3 lines (and optionally the two pre-existing broken URLs in the reference copy, and the nits), and this is an APPROVE ready to tag.

```json
{
  "verdict": "REQUEST_CHANGES",
  "findings": {
    "P1": [],
    "P2": [],
    "P3": [
      "v020-R2-P3-1: P3-4 sweep incomplete — the deployed docs page site/src/content/docs/installation.mdx still pins LAMBO_VERSION=0.1.0 (line 34) and still lists the unbuilt macOS x86_64 platform (line 12); docs.yml builds site/, so this is the page users see (site/README.md requires re-copying from docs/reference/, which was fixed but not re-copied). Also stale: scripts/aws-infra/README.md:164 documents the launch default as 0.1.0 (this README ships in the crates.io tarball) and launch_exhibit_ec2.py:38 usage docstring shows --lambo-version 0.1.0. Fix: 0.2.0 + drop macOS x86_64 in the site copy; repair the two pre-existing broken URLs in docs/reference/installation.mdx:17,32 before re-copying; bump the aws-infra prose."
    ],
    "nits": [
      "v020-R2-N1: scripts/install.sh:12 comment example still 'e.g. \"0.1.0\"' for LAMBO_VERSION (generic example, harmless).",
      "v020-R2-N2: brief count deltas — tarball ships 15 README paths = 12 distinct (root + 11; dev-diary/evidence/* are symlink dups), and exactly one distinct /home/nryn path (evidence/mcp-client-stdio/README.md:25).",
      "v020-R2-N3: stale bytecode scripts/aws-infra/__pycache__/launch_exhibit_ec2.cpython-314.pyc embeds 0.1.0 (untracked, not shipped, auto-regenerated).",
      "v020-R2-N4: pre-existing out-of-scope, non-blocking: docs/reference/installation.mdx:17,32 broken URLs (non-deployed copy; live site copy has correct URLs) and ci.yml path filters omitting web/** (a fourth include_str! input tree)."
    ]
  },
  "summary": "Six of seven remediations verified genuine with no regressions: whitelist package (103 files, 3.0 MiB, verify build green, zero creds/identifiers; nested-README residual proven cargo-mandated and non-suppressible, content clean), fixtures matrix complete (55 fixtures-gated lib tests + file-gated tests/rebuild_session.rs run in check/sqlite rows; no-default and demo/cockroach-live rows unchanged; check row, sqlite row, and all-features 820/0/8 all pass locally), notes render clean (no 0.1.0/macos-x86_64/placeholder), toolchain pin honest, publish-crate job correct, version consistent at 0.2.0 everywhere in the release path except the one sweep. One P3 blocks a clean APPROVE: the P3-4 version sweep missed the DEPLOYED docs page (site/src/content/docs/installation.mdx:34 pins 0.1.0; line 12 lists unbuilt macOS x86_64) plus two stale 0.1.0 in the aws-infra tree (README.md:164 ships in the crates tarball; launch docstring :38). Apply the ~3-line fix (and repair the two pre-existing broken URLs in docs/reference/installation.mdx before the documented re-copy, plus the nits) and the worktree is ready to tag v0.2.0."
}
```
