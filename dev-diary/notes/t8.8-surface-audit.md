# T8.8 — surface audit: help text + rustdoc

Produced by the T8.8 docs-verification pass on `task/t8.8-docs` (branched from
`phase/p8-surface` @ 34c9959). **Findings only — no `src/` was edited.** Each item names
the owning task. T8.8 verifies the inline help; it does not author it.

Binary under test: `cargo build --features store-sqlite` at 34c9959.
Gates on that tree: `cargo fmt --all -- --check` clean; `cargo test --features store-sqlite`
**678 passed, 0 failed, 3 ignored** across 11 suites.

This file lives in `dev-diary/` rather than `docs/reference/` on purpose: it carries task
ids and internal references, which the user-facing reference pages must not.

---

## 0. Triage — post-merge delta pass (`task/t8.8-delta`, 2026-08-15)

Branched from `8134a3c` ("Merge task/live-l82-remediation"). This pass owns
`docs/reference/**`, `dev-diary/**`, and **comment-only** rustdoc edits in `src/` **excluding
`src/mcp/**`**, which a parallel branch holds. Sections 1–3 below are the original findings and
are left as written; this table is the disposition of each.

| Finding | Surface | Disposition |
|---|---|---|
| T88-H1 | `src/mcp/server.rs` | **Assigned** — being fixed on the parallel `src/mcp` branch. Not touched here. |
| T88-H2 | `src/mcp/server.rs` | Pending T8.7 branch. `spec §2.2` in a published arg description is a code change. |
| T88-H3 | `src/mcp/server.rs` | Pending T8.7 branch for the wire text. **Docs half done**: `mcp.mdx` "How `agent_id` is used" states the rule for all three cases. |
| T88-H4 | `src/mcp/server.rs` | Pending T8.7 branch for the schema. **Docs half done**: `mcp.mdx` "Limits" now warns that the published `top_k` minimum of `0` contradicts the runtime `1..=100`, so a schema-generated client must not send `0`. |
| T88-H5 | — | No action. Verified consistent. |
| T88-H6 | `src/store` / lease error | Pending T8.3. The message is a string literal, so it is a code change and out of this pass's comment-only scope. `cli.mdx` keeps quoting it verbatim, because it is what users see today. |
| T88-H7 | `src/cli/inspect.rs` | Pending T8.3. Doubled `inspect: inspect:` prefix is a code change. `cli.mdx` quotes the doubled form verbatim for the same reason. |
| T88-H8 | `src/cli/caps.rs` | **Fixed here.** Comment-only, and the constant is in this pass's surface. `MAX_INSPECT_NODES` is a **total** budget across all hops, not per frontier level. Confirmed at `src/cli/inspect.rs:146`, where `budget` is initialised *before* the `for hop` loop. Both `cli.mdx` and `mcp.mdx` now state the total explicitly. |
| T88-H9 | `src/main.rs` clap `about` | Pending T8.4 / T8.5. **Docs half already done**: `cli.mdx` says the command is not built yet. |
| T88-H10 | `src/cli/reserve.rs` | Pending T8.3. **Docs half already done**: `cli.mdx` explains the RAM-local lifetime and the resulting `release` failure in one warning. |
| T88-H11 | — | No action. Verified consistent. |

### L82 delta — done

All three `{/* L82 delta pass pending … */}` markers are removed. Per page:

- **`mcp.mdx`** — new "What Lambo accepts in text" section (two refused classes, the allowed
  text-required invisibles, and the no-duplicate-concepts guarantee) with the verbatim refusal
  as a wire example; `lambo_recall` gains the per-store recall-leg table and the statement that
  derived concepts keep their embedding; error table splits size from character refusals.
- **`cli.mdx`** — same character section in CLI terms with both verbatim refusals and a live
  no-fork demo; `recall` gains the per-store sentence; new "Stopping a writer" section covering
  the batched flush and the honest abandoned-tail loss; `inspect` total-vs-per-level clarified.
- **`end-to-end.mdx`** — batched flush in "The model"; new "What recall searches" section with
  the leg matrix; shutdown paragraph rewritten and paired with the abandoned-tail warning.

Verified live against a `cargo build --features store-sqlite` binary at `8134a3c`, sqlite store
plus fixture embedder: ten refusal/acceptance cases (`U+202E`, `U+0001`, `U+200B`, `U+FEFF`,
`U+3164`, `U+E0061` refused with exit `2`; tab/newline, a ZWJ emoji family, `U+FE0F`, and
`U+0600` accepted) and the no-fork check — a ZWJ or `U+FE0F` inserted into an existing concept's
text returns `0 created, 1 matched existing`.

Per-store vector claims are pinned to `capabilities()`: CockroachDB returns
`Capabilities::VECTOR_SEARCH`, SQLite and the in-memory store return `Capabilities::empty()`.

**Invisible characters are never pasted literally into the `.mdx`.** A raw `U+202E` in a docs
page would reorder the rendered line and hide itself from the next reader, which is the exact
defect the feature exists to prevent. Examples build their input with `printf` escapes instead,
and a repo-wide scan confirms no raw invisible codepoint in `docs/reference/`.

---

## 1. Help-text findings — MCP (owner: T8.2)

### T88-H1 — internal developer notes are published on the wire (highest impact)

`WireConceptType`'s rustdoc (`src/mcp/server.rs:50-72`) becomes the enum's JSON-Schema
`description` in every `tools/list` response. Captured verbatim from a live stdio session,
the published text includes:

> Byte-echo note (R4 nit): an invalid value here yields serde's `unknown variant …` error …
> This is **not** interceptable at our layer: every tool takes its params through rmcp's
> `Parameters<T>` extractor … revisit if rmcp grows an extraction-error hook.

Every MCP client and every model sees this. It names internal review findings, an internal
helper (`validate_size`), and a dependency's internals. Recommend a short user-facing
`description` on the wire, with the rationale moved to a non-doc comment.

### T88-H2 — `spec §` reference in a published argument description

`RecallParams.agent_id` (`src/mcp/server.rs:98`) publishes
`"Calling agent (spec §2.2 — see the attribution note in the tool docs)"`. Both references
are meaningless to a client: there is no spec on the wire, and there are no "tool docs" the
client can reach.

### T88-H3 — the attribution rule is not in any tool description

A foreign `agent_id` on the six non-reserve tools is accepted, the work is recorded under
the process's agent, and a warning is attached. Only `lambo_reserve`'s description mentions
its (stricter) identity rule. A model reading descriptions alone would reasonably assume
`agent_id` attributes the write. The reference page now states it, but the wire text should
carry at least a sentence.

### T88-H4 — published schemas carry no maxima, and one contradicts the runtime

`tools/list` publishes `top_k`, `max_tokens`, `traversal_depth`, `depth`, `ttl_seconds` as
`{"format":"uint","minimum":0,"type":["integer","null"]}` with **no `maximum`**, and string
fields with no `maxLength`. Runtime enforces `top_k` 1..=100, `max_tokens` 1..=100_000,
depths 0..=5, ttl 1..=3600, strings <= 16384 bytes.

Two consequences: a client cannot pre-validate, so every out-of-range call costs a round
trip; and `top_k`'s published `minimum: 0` **contradicts** the runtime minimum of 1
(`top_k: 0` is refused with `top_k must be in 1..=100`).

### T88-H5 — verified consistent, no action

`lambo_stats`, `lambo_saints`, `lambo_derive`, `lambo_record_action`, `lambo_inspect` and
`lambo_reserve` descriptions match observed behavior. `get_info` instructions match the
binary verbatim (captured from `initialize`). The "never send a timestamp" instruction is
enforced: a client `created_at` is refused by `deny_unknown_fields`.

---

## 2. Help-text findings — CLI (owner: T8.3, except T88-H9)

### T88-H6 — user-facing lease error carries a spec reference and raw SQL

Verified live (exit 1):

```
lambo derive: conflict: session demo is already held by another writer
(agent-a@host#10492) — it acquired the single-writer lease 66s ago and is still refreshing
it. Spec §2.2 is one writer per session; refusing to open a second. If that holder is
wedged, an operator can force a takeover: DELETE FROM session_leases WHERE session_id =
'<session>';
```

The holder identity and the refusal are excellent. Two problems: `Spec §2.2` means nothing
to a user, and a raw `DELETE` is operator surgery offered in a message that any routine
agent invocation triggers. Recommend dropping the spec citation and moving the SQL behind a
pointer. The reference page quotes this verbatim today because it is what users will see.

### T88-H7 — doubled prefix in the ambiguity error

```
lambo inspect: inspect: 'a' matches 5 concepts — name one exactly, or pass its node_id:
```

The command prefix is applied twice (`src/cli/inspect.rs` message already begins `inspect:`,
and the dispatcher adds `lambo inspect: `). Verified live.

### T88-H8 — `MAX_INSPECT_NODES` doc-comment contradicts the code

`src/cli/caps.rs:30` says "Cap on neighbours rendered **per `inspect` frontier level**".
`render_neighbourhood` (`src/cli/inspect.rs:146`) initialises one `budget = MAX_INSPECT_NODES`
**before** the hop loop and decrements it across all hops, so 200 is a **total**, not
per-level. The doc-comment is wrong; the code is fine. Reference pages state the total.

### T88-H9 — `lambo demo` help describes behavior that does not exist (owner: T8.4 / T8.5)

`--help` says "Scripted two-agent demo scenario". The implementation
(`src/main.rs:388-392`) is a stub: it prints `lambo demo (stub): scenario=rest-api` and
exits 0 without touching memory. `--scenario` has no help text of its own and no list of
valid values. The reference page now says the command is not built yet.

### T88-H10 — `lambo release` after a CLI `reserve` reports a generic not-found

`reserve --help` and the runtime note both explain that a CLI reservation ends when the
process exits. The follow-up `release` then fails with
`lambo release: not found: no reservation on node <uuid>` (exit 1), which does not connect
back to that explanation. A message naming the cause would close the loop. Low severity:
the help text is accurate, only the error is terse.

### T88-H11 — verified consistent, no action

Every subcommand and every flag has help text. `--config` is correctly documented as global
on all subcommands. Concept-kind values, defaults (`--depth 2`, `--ttl-seconds 30`,
`--port 7700`, `--bind 127.0.0.1`, `--agent lambo-serve`, `--transport stdio`), the
`CHILD:PARENT` orientation, and the reader-vs-writer note on `stats` all match the binary.
Exit codes match the documented scheme (usage 2, runtime 1).

---

## 3. Rustdoc report (owner: post-L82-merge pass)

`cargo doc --no-deps --features store-sqlite,store-cockroach` exits 0 but emits
**53 warnings**. T8.8's "done when" requires a clean `cargo doc`, so this is an open gap.

### 3.1 By category

| Category | Count |
|---|---|
| Public documentation links to a **private** item | 30 |
| Unresolved intra-doc link | 11 |
| Ambiguous link (`derive` function vs attribute macro) | 9 |
| Ambiguous link (`crate::mcp::serve` function vs module) | 1 |
| Unclosed HTML tag `T` (a generic written bare in prose) | 2 |

### 3.2 By file

| File | Warnings |
|---|---|
| `src/memory.rs` | 11 |
| `src/graph/derive.rs` | 9 |
| `src/store/flush.rs` | 5 |
| `src/store/load.rs` | 4 |
| `src/mcp/serve.rs` | 4 |
| `src/mcp/server.rs`, `src/graph/reserve.rs`, `src/graph/mod.rs`, `src/graph/demote.rs`, `src/cli/reserve.rs` | 2 each |
| `src/store/cockroach.rs`, `src/graph/graph.rs`, `src/graph/canonical.rs`, `src/embed/fixture.rs`, `src/daemon/gc.rs`, `src/daemon/events.rs`, `src/daemon/conflict.rs`, `src/cli/saints.rs`, `src/cli/record_action.rs`, `src/cli/derive.rs` | 1 each |

### 3.3 Unresolved links (each needs a path fix or a plain-code-span)

`GraphSnapshot` (×2), `InvertedIndex`, `Mutation`, `epoch_after`, `split_sentence_bounds`,
`Memory::derive`, `Memory::record_action`, `Memory::reserve`, `Memory::release`,
`Memory::canonical_memories`.

The five `Memory::*` failures are notable: `src/lib.rs` re-exports `Memory`, so public
prose pointing at the primary API's own methods currently renders as dead text.

### 3.4 Private-item links (the largest group)

Public docs on `close`, `flush`, `serve`, `load`, `load_session`, `load_session_async`,
`retract`, `demote`, `canonical`, `conflict`, `cockroach`, `memory`, `stop`,
`session_stale_at`, `near_far_contract` and `apply_canonization_transition` link to private
items including `HandleCustody`, `TailCustody`, `POLL_QUANTUM`, `SHUTDOWN_GRACE`,
`SHUTDOWN_BUDGET`, `FLUSH_ATTEMPT_TIMEOUT`, `RETRACT_IO_TIMEOUT`, `LOAD_SESSION_TIMEOUT`,
`RETAINED_BACKOFF`, `Memory::writers`, `Memory::latch_success`, `Memory::mirror_concepts`,
`FlushLoop::cycle`, `canonical_key_for`, `edge_writers`, `dsn_for_rustls`, `STOPWORDS`,
`block_on`, `run_until_shutdown`, `close_bounded`, `legal_canonization_transition`,
`session_last_activity`, `DEFAULT_FIXTURE_DIM`, `load_session_with_timeout`.

These are readable in-tree but render as dead links on the published docs. The usual fixes
are to demote the reference to a code span, or to make the constant public where it is part
of the contract being described (several timeouts arguably are).

### 3.5 Undocumented public items

`RUSTDOCFLAGS="-W missing_docs"` reports **334** missing doc comments. The lint is not
enabled in the crate, so these are not part of the 53 above.

| Kind | Count |
|---|---|
| struct field | 195 |
| enum variant | 54 |
| method | 37 |
| struct | 19 |
| associated function | 12 |
| enum | 8 |
| constant | 6 |
| associated constant, type alias, function | 1 each |

Concentrated in `src/types/mod.rs` (159), `src/config.rs` (30), `src/mcp/server.rs` (22),
`src/graph/graph.rs` (21), `src/memory.rs` (14).

`src/types/mod.rs` alone is roughly half. Since `src/lib.rs` re-exports these, they are the
first thing a library reader meets. `src/config.rs`'s 30 are the `Config` fields the
reference page now documents by hand — a good candidate for the first pass, since the prose
already exists.

---

## 4. Not done here, deliberately

- **No `src/` edits.** Two branches (`task/live-l82-remediation`, `task/l82-4-fresh-embeddings`)
  are in review over `src/mcp/serve.rs`, `src/store/{flush,cockroach,sqlite,batch}.rs`,
  `src/cli/caps.rs`, `src/graph/hybrid.rs`, `src/memory.rs`. Every fix above lands after they
  merge.
- **L82 behavior is not documented yet.** `docs/reference/mcp.mdx`, `cli.mdx` and
  `end-to-end.mdx` carry a `{/* L82 delta pass pending: control-char ranges, organic vector
  recall, bulk flush */}` marker at the top. (MDX, not HTML comments: `<!-- -->` is not valid
  MDX and breaks the Mintlify build.)
- **T8.7 knobs are not documented.** `config.mdx` still says the HTTP transport has no
  authentication, rate limiting, or session cap, which matches the binary at 34c9959. Revisit
  when T8.7 lands.
