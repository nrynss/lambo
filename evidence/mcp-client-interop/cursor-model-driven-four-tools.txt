Cursor Agent CLI 2026.08.11-e8db854 — model-driven lambo MCP tool calls
Captured 2026-08-15. Account: rocknarayan@gmail.com (cursor-agent status: Logged in).

Workspace: a scratch directory, NOT the repo. It held only two files, both written
for this test: lambo-mcp-test.toml (store=memory, embedder=fixture, dim=1024) and
.cursor/mcp.json registering the `lambo` stdio server. Same store/embedder config as
the earlier stdio evidence, so results are comparable.

.cursor/mcp.json:
{
  "mcpServers": {
    "lambo": {
      "command": "/home/nryn/work/lambo/target/debug/lambo",
      "args": ["--config", "<SCRATCH>/lambo-mcp-test.toml",
               "serve", "--session", "t9-cursor-evidence",
               "--agent", "agent-a", "--transport", "stdio"]
    }
  }
}

Approval notes — both were required, and both are real findings for anyone scripting
this:
  1. `cursor-agent mcp enable lambo` is needed first, or list-tools fails with
     'MCP server "lambo" has not been approved'.
  2. `--approve-mcps` alone approves LOADING the server but NOT individual tool
     calls. In non-interactive print mode each call is then auto-rejected with
     "User rejected MCP: lambo-lambo_derive". The run below added --force, with
     --sandbox enabled to bound it.

$ cd <SCRATCH> && cursor-agent -p --trust --approve-mcps --sandbox enabled --force \
    --output-format text "<four-step prompt: derive, record_action, recall, stats;
    agent_id agent-a; lambo MCP tools only>"

--- agent output, verbatim ---

Here are the results for each step (all calls used `agent_id: "agent-a"`). No errors were returned.

### Step 1: `lambo_derive`
Arguments:
- concepts: `user schema` (entity), `add oauth_id to user schema` (constraint),
  `add rate limiting middleware` (constraint)
- parent_of: both children linked to `user schema`
Response:
    derived 3 concept(s): 3 created, 4 matched existing

### Step 2: `lambo_record_action`
Arguments:
- action: `implement oauth login`
- modifies: ["user schema"]
Response:
    recorded action 'implement oauth login': 1 concept(s) created, 1 edge(s) added

### Step 3: `lambo_recall`
Arguments:
- query: `user schema`
- top_k: 5
Response:
    user schema [Entity] (score 1.12)

    add oauth_id to user schema [Constraint] (score 0.84)

    add rate limiting middleware [Constraint] (score 0.50)

    implement oauth login [Resource] (score 0.49)

(4 hits returned; top_k was 5.)

### Step 4: `lambo_stats`
Arguments: agent_id only
Response:
    session 't9-cursor-evidence' (owner agent 'agent-a')
    nodes=6 edges=11 concepts=4 canonical=0
    flush_lag=3.620154454s log_depth=0 flush_depth=0 dead_lettered=0 degraded=false
    epoch=18 daemon_cycles=18 canonization_cycles=0 canonization_failures=0

Errors: None — all four calls succeeded.

--- end verbatim ---


================================================================================
RE-VERIFICATION — 2026-08-15, later the same day

The claim "--approve-mcps approves loading the server but not the individual tool
calls" was challenged as possibly stale, on the grounds that the Cursor account had
been authenticated since the original capture. It was re-tested from scratch: a fresh
scratch workspace, a newly written .cursor/mcp.json, and cursor-agent 2026.08.11-e8db854
(the same build). Both gotchas still reproduce.

$ cursor-agent mcp list          # before enabling
aws-mcp: Error: Connection failed
cockroachdb-cloud: requires_authentication
lambo: not loaded (needs approval)

$ cursor-agent mcp list-tools lambo          # without `mcp enable` first
Failed to list tools: Failed to load MCP 'lambo': MCP server "lambo" has not been approved

$ cursor-agent mcp enable lambo
✓ Enabled and approved MCP server: lambo

$ cursor-agent mcp list-tools lambo
Tools for lambo (7):
- lambo_derive (agent_id, concepts, parent_of)
- lambo_inspect (agent_id, depth, focus)
- lambo_recall (agent_id, max_tokens, query, top_k, traversal_depth)
- lambo_record_action (action, agent_id, depends_on, modifies, produces)
- lambo_reserve (agent_id, node_id, release, ttl_seconds)
- lambo_saints (agent_id)
- lambo_stats (agent_id)

Then the actual question — the same style of prompt, WITHOUT --force:

$ cursor-agent -p --trust --approve-mcps --sandbox enabled --output-format text \
    "Using only the lambo MCP tools with agent_id \"agent-a\": 1) lambo_derive ...
     2) lambo_recall ... 3) lambo_stats. Report each tool's raw response."

--- agent output, verbatim (tail) ---

The lambo MCP calls were rejected in the IDE, so I couldn't run them and don't have
raw responses to report.
[... the agent then printed the exact arguments it had chosen for all three calls ...]
**Status:** `lambo_derive` was rejected twice (`User rejected MCP: lambo-lambo_derive`),
so steps 2 and 3 were not run.

CONCLUSION: unchanged. Authentication is not the variable. `--approve-mcps` covers
loading the server; individual tool calls are still auto-rejected in print mode, and
--force is still required. Worth noting what the failure looks like: the model routed
to the right tools and built correct arguments, then had every call rejected — so a
rejection here is an approval-layer result, not a discovery or schema problem.
